# --- Gemini (Google Cloud) — pick ONE of the two auth modes ---
# Mode A: Gemini API key (fastest to start) — https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Mode B: Vertex AI (recommended on Cloud Run; uses service-account identity)
# GOOGLE_GENAI_USE_VERTEXAI=true
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=us-central1

GEMINI_MODEL=gemini-2.5-flash

# --- GitLab webhook verification (same DNA as guard-agent-test) ---
GITLAB_WEBHOOK_SECRET=whsec_replace_me

# --- Persistence ---
# GUARDAGENT_DB=/data/guardagent.db
